namespace Family.Vault.UI.Models;

/// <summary>
/// Country for a property — mirrors <c>Family.Vault.Domain.Enums.PropertyCountry</c>
/// so the UI project remains independent of the Domain assembly.
/// </summary>
public enum PropertyCountry
{
    UK,
    India
}
